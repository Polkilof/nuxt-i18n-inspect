/**
 * fallbackLocale читається ТІЛЬКИ звідси: в опціях модуля @nuxtjs/i18n
 * такого поля немає, а без цього файлу воно примусово стає false.
 *
 * Для демо це принципово: половина ключів у de відсутня, і саме фолбек
 * робить їх клікабельними - на екрані англійський текст, а маркер несе
 * правильний ключ.
 */
export default defineI18nConfig(() => ({
  fallbackLocale: 'en',
  // без них vue-i18n пише по два попередження на кожен відсутній ключ
  missingWarn: false,
  fallbackWarn: false,
}))
